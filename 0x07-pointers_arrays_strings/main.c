#include "main.h"

char *_memset(char *s, char b, unsigned int n) {
// Your implementation of _memset goes here.
// It should fill the first 'n' bytes of the memory pointed by 's' with the value 'b'.
// Return the pointer to the memory area 's'.

return s;
}
